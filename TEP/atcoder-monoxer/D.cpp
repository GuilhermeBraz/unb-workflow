#include<iostream>
#include<math.h>

using namespace std;

int solve (int a, int s)
{
    for(; ;)
    {
        if()
    }
}

int main ()
{   
    int T, a, s;

    cin >> T;

    for (int i = 0; i < T; i++)
    {
        cin >> a >> s;
        solve(a, s);
    }
    

    return 0;
}